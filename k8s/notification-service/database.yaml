# MongoDB Deployment for Notification Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-mongodb
  namespace: electromart
  labels:
    app: notification-mongodb
    service: notification-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-mongodb
  template:
    metadata:
      labels:
        app: notification-mongodb
    spec:
      containers:
      - name: mongodb
        image: mongo:7.0
        ports:
        - containerPort: 27017
        env:
        - name: MONGO_INITDB_ROOT_USERNAME
          value: "admin"
        - name: MONGO_INITDB_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: notification-service-secrets
              key: MONGODB_PASSWORD
        - name: MONGO_INITDB_DATABASE
          value: "electromart_notifications"
        volumeMounts:
        - name: notification-mongodb-storage
          mountPath: /data/db
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          exec:
            command:
            - mongosh
            - --eval
            - "db.adminCommand('ping')"
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - mongosh
            - --eval
            - "db.adminCommand('ping')"
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: notification-mongodb-storage
        persistentVolumeClaim:
          claimName: notification-mongodb-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: notification-mongodb
  namespace: electromart
  labels:
    app: notification-mongodb
    service: notification-service
spec:
  selector:
    app: notification-mongodb
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
  type: ClusterIP
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: notification-mongodb-pvc
  namespace: electromart
  labels:
    service: notification-service
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
